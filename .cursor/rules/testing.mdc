---
description: Proactive test and lint verification — run, fix, repeat
globs: "{test/**,lib/**,server/**,run*.sh,scripts/**}"
alwaysApply: false
---

# Testing — Verify Before Done

## API target (기본: NCP 서버)

- **기본:** `http://api.iljari.app:8000` — `scripts/remote_api.env` SSOT
- **앱/UI:** 맥에서 `run_*.sh` / `flutter test` (클라이언트만 로컬)
- **통합·QC·E2E:** NCP PostgreSQL + Docker API — `./scripts/seed_ncp_server.sh`
- **로컬 API (opt-in):** `ILJARI_API_MODE=local ./run_qc.sh`

```bash
curl -sf http://api.iljari.app:8000/health
ILJARI_QC_SEEKERS=100 ./scripts/seed_ncp_server.sh
```

Admin 키 (NCP `.env`와 동일): `iljari-admin-dev-key`

## Default commands

```bash
cd ~/Projects/iljari-app
flutter test
flutter analyze
```

Run **targeted tests first**, then full suite when touching shared core, pricing, or auth.

### Targeted examples

```bash
flutter test test/features/corporate/corporate_home_shell_test.dart
flutter test test/features/corporate/
flutter test test/core/compliance/
```

## When to add tests

- User requests tests, OR
- Bug fix with clear repro → add regression test, OR
- New business rule (tier limits, payment gating, commission math).

Do **not** add trivial widget existence tests.

## Widget tests

- Use `SharedPreferences.setMockInitialValues({})` + `AuthSession.instance.signOut()` in tearDown.
- Scroll before tap if home shell content is below fold (`scrollUntilVisible` / `ensureVisible`).
- Unit/widget tests는 mock — **실 API 호출 금지** (NCP 의존 없음).

## Fix loop

1. Run failing test → read stack trace.
2. Fix root cause (not assertion-only hacks).
3. Re-run failed file → then related folder → then full `flutter test` if high risk.
4. Record result in `workflow_state.md` → **Verification**.

## Lint

- After substantive edits: `ReadLints` on touched paths or `flutter analyze`.
- Zero new analyzer errors in changed files before marking task complete.

## Server

```bash
cd server && python -m pytest
```

Run when changing `server/app/` routes or compliance logic. 배포 후 NCP에서 smoke:

```bash
curl http://api.iljari.app:8000/health
```

## CI mindset

- Prefer green suite over skipping tests.
- Mock external APIs (PG, OCR, maps) — never require real keys in unit tests.
- Integration QC는 NCP 서버 기준 — 로컬 uvicorn 시드는 `ILJARI_API_MODE=local`일 때만.
